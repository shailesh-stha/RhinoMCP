---
description: Capture the current Rhino viewport and describe what's on screen.
---

Use the `mcp__rhino__get_viewport_image` tool to capture the active Rhino viewport, then give the user a brief description of what is visible — geometry, layers, framing, and anything that looks off (empty view, clipped objects, unusual camera angle).

Keep the description to 3-4 sentences unless the user asks for more detail.
